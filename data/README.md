# Runtime data

Copied from `engine/data/` at click install time. Maintainers: use `touch/xonotic.cfg` for port defaults; fetch game assets with `./scripts/fetch-sources.sh full` (testers) or `code` (QuakeC tree includes pk3dir assets).

Nothing here is committed except this README.
