# Build outputs — never commit; maintainers do not compile locally
build/
.clickable/
bin/

# Accidental compile artifacts inside fetched trees
engine/darkplaces/build-obj/
engine/darkplaces/darkplaces-sdl
engine/darkplaces/darkplaces-sdl-release
engine/darkplaces/darkplaces-sdl-debug
engine/darkplaces/darkplaces-sdl-profile
engine/gmqcc/gmqcc
engine/d0_blind_id/.libs/

# Fetched upstream (clone with scripts/fetch-sources.sh)
engine/

# Runtime data copies
data/*
!data/README.md

# Optional: commit unified diffs here instead of whole engine trees
# !patches/
