# engine/ directory

Populated by `./scripts/fetch-sources.sh` — not stored in this git repo.

After clone, upstream's own `engine/README.md` replaces any placeholder. For port workflow see [MAINTAINING.md](MAINTAINING.md).
